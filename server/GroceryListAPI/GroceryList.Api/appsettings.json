{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "CorsOrigins": "http://localhost:4200",
  "ConnectionStrings": {
    // Please note that this connection string is only included for demonstration purposes.
    // Connection strings, especially those that contain sensitive data, should never be stored in source control.
    "localdb": "Server=(localdb)\\mssqllocaldb;Database=GroceryList;Trusted_Connection=True;MultipleActiveResultSets=true"
  }
}
